# DoctorFizz Keyword Rank Checker

Deployment-ready SerpApi-only build of the DoctorFizz rank checker.

Search-provider architecture:

Browser UI → PHP API → SerpApi → Google organic results → domain match → rank result

The search layer is SerpApi-only.
